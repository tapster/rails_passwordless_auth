<div class="max-w-md items-center justify-center w-full space-y-8">
  <div>
    <h2 class="mb-6 text-slate-700 font-extrabold text-3xl sm:text-4xl lg:text-5xl text-center">Lets get started</h2>
    <p class="mt-2 text-center text-sm text-gray-600">To keep your account secure, we need to confirm your email. We'll send a verification code to the email address you enter here.</p>
  </div>
  <div class="overflow-hidden rounded-lg bg-white shadow mb-4">
    <div class="border-b border-gray-200 bg-white px-2 py-2 sm:px-4">
      <%= form_with(url: auth_path, class: "mt-2 space-y-6") do |f| %>
        <div class="rounded-md shadow-sm -space-y-px">
          <div>
            <%= f.email_field :email, value: params[:email], placeholder: "Email address", required: true, class: "mb-4 block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md sm:text-sm" %>
          </div>
        </div>
        <div>
          <button name="button" type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-blue-500">Send Code</button>
        </div>
      <% end %>
    </div>
  </div>
</div>
